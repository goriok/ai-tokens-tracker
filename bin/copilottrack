#!/bin/bash
# Drop-in replacement for the removed Python scripts/copilot-track.py — runs
# a copilot -p task and records its token usage.
set -e
source "$(dirname "$(readlink -f "$0")")/_resolve.sh"
exec $AITOKENS_EXPORTER_BIN track --tool copilot "$@"
