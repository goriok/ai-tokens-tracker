#!/bin/bash
# Drop-in replacement for the removed Python scripts/agy-delegate.py — picks
# a model by task complexity + current agy quota, runs it, records the call.
set -e
source "$(dirname "$(readlink -f "$0")")/_resolve.sh"
exec $AITOKENS_EXPORTER_BIN delegate "$@"
